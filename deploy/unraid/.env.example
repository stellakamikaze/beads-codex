# ============================================
# Codex + Claude Code Stack - Environment Variables
# ============================================
# Copia questo file come .env e compila i valori

# --------------------------------------------
# CODEX TOKEN (Required)
# --------------------------------------------
# Token condiviso per autenticazione API Codex
# Genera con: openssl rand -hex 32
CODEX_TOKEN=

# --------------------------------------------
# CLAUDE CODE MAX (Credentials via file mount)
# --------------------------------------------
# Claude Code MAX usa OAuth, non API key.
# Copia le credenziali dal tuo PC a Unraid:
#
# Windows:
#   xcopy %USERPROFILE%\.claude\* \\unraid\appdata\claude-code\config\ /E
#
# Mac/Linux:
#   scp -r ~/.claude/* root@unraid:/mnt/user/appdata/claude-code/config/
#
# I file necessari sono:
#   - credentials.json (token OAuth)
#   - settings.json (configurazione)

# --------------------------------------------
# UNRAID API (Optional - per Unraid MCP)
# --------------------------------------------
# URL GraphQL di Unraid (default: http://tower.local/graphql)
UNRAID_API_URL=http://tower.local/graphql

# API Key di Unraid (se configurata)
# Ottieni da: Settings > Management Access > API Key
UNRAID_API_KEY=
